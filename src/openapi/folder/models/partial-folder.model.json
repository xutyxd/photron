{
    "type": "object",
    "required": [ "name", "description" ],
    "properties": {
        "parentIndex": {
            "type": "number",
            "example": "1"
        },
        "name": {
            "type": "string",
            "example": "Folder name"
        },
        "description": {
            "type": "string",
            "example": "Folder description"
        }
    }
}